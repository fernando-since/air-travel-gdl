<a href="https://wa.me/+523332548806">
    <button class="button">
        <span class="button-content" data-translate="CONTACT_US">Contactanos</span>
    </button>
</a>
  

<style>

.button {
  position: relative;
  overflow: hidden;
  height: 3rem;
  margin: 25px 0;
  padding: 0 2rem;
  border-radius: 1.5rem;
  background: var(--text-color);
  background-size: 400%;
  color: var(--color-bg);
  border: none;
  cursor: pointer;
}

.button:hover::before {
  transform: scaleX(1);
}

.button:hover {
  scale: 1.015;
  box-shadow: 0 0px 20px rgba(193, 163, 98,0.4);
}


.button-content {
  position: relative;
  z-index: 1;
}

.button::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  transform: scaleX(0);
  transform-origin: 0 50%;
  width: 100%;
  height: inherit;
  border-radius: inherit;
  /* background: linear-gradient(
    82.3deg,
    var(--color-primary) 10.8%,
    var(--color-primary) 94.3%,
  ); */
  background: linear-gradient(
    82.3deg,
    rgb(110 79 149) 10.8%,
    rgba(99, 88, 238, 1) 94.3%
  );
  transition: all 0.475s;
}


</style>