<section
  class="section-container place-items-center pb-24 lg:pt-4 lg:pb-24 section-hero">
  <div class="flex flex-col lg:flex-row justify-between items-center lg:min-h-screen lg:p-8">
    <div class="card-left grid justify-center p-5 mb-10 lg:ml-28 lg:mb-80 text-white content-center bg-black bg-opacity-20 backdrop-blur-[2px] rounded-md">
      <div class="flex items-center lg:space-x-2">
        <p class="text-5xl md:text-7xl lg:text-4xl font-bold title">PINTURAS</p>
        <img src="/assets/logos/logo_blanco.svg" alt="Logo" class="icon-img">
      </div>
      <p class="flex -mt-2 lg:-mt-6 text-5xl md:text-7xl lg:text-4xl font-bold title">COLOR EXPLOSIÓN</p>
      <div class="flex flex-wrap flex-col sm:flex-row items-center">
        <span class="font-light text-base sm:text-lg md:text-2xl lg:text-3xl xl:text-3xl">
          Sucursal 5 de febrero #1085
        </span>
        <span class="flex items-center font-light text-base sm:text-lg md:text-2xl lg:text-3xl xl:text-3xl mt-2 sm:mt-0 sm:ml-2">
          <i class="fab fa-whatsapp w-auto mr-2"></i>
          3333049075
        </span>
      </div>            
    </div>

    <div class="card-right lg:mb-80 p-5 md:p-8 lg:p-10 text-white content-center bg-black bg-opacity-20 backdrop-blur-[2px] rounded-md">
      <p class="text-xl md:text-4xl lg:text-3xl font-semibold offer-text">Somos distribuidores oficiales de Axalta.</p>
      <hr class="my-6 md:my-11 lg:my-10 border-stone-300"/>
      <p class="text-lg lg:text-2xl p-2 text-desc">Desde línea de estética automotriz, hasta complementos especializados para  el laminero, pintor y detallador automotriz. </p>
    </div>
  </div>
</section>

<style>
  .section-container {
    margin-top: 4rem;
   }

  .card-left {
    width: 100%;
    max-width: 30%;
    height: 275px;
  }

  .card-right {
    width: 100%;
    max-width: 32%;
    height: 405px;
  }

  .icon-img {
    height: 6rem;
    width: auto;
    --tw-translate-y: -2rem;
    transform: translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));
  }

  /* Media Query for Laptops and Desktops */
  @media (min-width: 1025px) and (max-width: 1280px) {
    .card-left {
      width: 100%;
      max-width: 40%;
      height: 275px;
    }

    .card-right {
      width: 100%;
      max-width: 40%;
      height: 505px;
    }
  }

  /* Media Query for Tablets Ipads portrait mode */
  @media (min-width: 768px) and (max-width: 1024px) {
    .card-left {
      width: 100%;
      max-width: 70%;
      height: 375px;
    }

    .card-right {
      width: 100%;
      max-width: 70%;
      height: 405px;
    }

    .icon-img {
      height: 6rem;
      width: auto;
      --tw-translate-y: -1rem;
      transform: translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));
    }
  }

  /* Media Query for low resolution  Tablets, Ipads */
  @media (min-width: 481px) and (max-width: 767px) {
    .card-left {
      width: 100%;
      max-width: 80%;
      height: 275px;
    }

    .card-right {
      width: 100%;
      max-width: 80%;
      height: 305px;
    }

    .icon-img {
      height: 4rem;
      width: auto;
      --tw-translate-y: -1rem;
      transform: translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));
    }
  }

  /* Media Query for Mobile Devices */
  @media only screen and (max-width: 480px) {
    .card-left {
      width: 100%;
      max-width: 80%;
      height: auto;
    }

    .card-right {
      width: 100%;
      max-width: 80%;
      height: auto;
    }

    .icon-img {
      height: 3.50rem;
      width: auto;
      --tw-translate-y: -0.75rem;
      transform: translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));
    }
    
    .section-container {
      margin-top: 10px !important;
    }
    .offer-text {
      font-size: 18px;
    }

    .text-desc {
      font-size: 18px;
    }

    .title {
      font-size: 40px;
    }
	}
</style>
